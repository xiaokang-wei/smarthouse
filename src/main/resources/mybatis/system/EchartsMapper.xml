<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.system.echarts.mapper.EchartsMapper">

    <select id="getData" resultType="integer">
        select list_amount from sell_list
    </select>
    <select id="getData1" resultType="string">
        select list_name from sell_list
    </select>
</mapper>